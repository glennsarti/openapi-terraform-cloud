name: "filter[operation]"
description: List runs with the specified operations
in: "query"
style: form
explode: false
schema:
  type: array
  items:
    type: string
    enum:
      - plan_only
      - plan_and_apply
      - refresh_only
      - destroy
      - empty_apply
