description: Show Cost Estimate Response

content:
  application/vnd.api+json:
    schema:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '../schemas/CostEstimate.yaml'
