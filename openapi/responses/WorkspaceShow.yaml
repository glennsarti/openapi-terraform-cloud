description: Show Workspace Response

content:
  application/vnd.api+json:
    schema:
      type: object
      required:
        - data
      properties:
        data:
          $ref: '../schemas/Workspace.yaml'
