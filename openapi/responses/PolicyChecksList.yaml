description: List Policy Checks Response

content:
  application/vnd.api+json:
    schema:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          items:
            $ref: '../schemas/PolicyCheck.yaml'
        meta:
          type: object
          properties:
            pagination:
                $ref: '../schemas/MetaPagination.yaml'
